services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.standalone
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    volumes:
      - tmp_data:/tmp

volumes:
  tmp_data:
